{
  "name": "udg",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],

  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "dependencies": {
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
