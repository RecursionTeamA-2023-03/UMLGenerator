---
title: 'ホテル予約サイト'
diagram: 'ユースケース図'
ans: |
  @startuml
  left to right direction
  actor "顧客" as customer
  actor "管理者" as admin
  rectangle "ホテル予約システム" {
    customer --> (ホテル検索)
    customer --> (予約)
    customer --> (キャンセル)
    customer --> (変更)
    admin --> (ホテル情報編集)
    admin --> (ホテル情報削除)
    admin --> (ホテル情報追加)
    admin --> (ホテル情報表示)
    (ホテル検索) --> (部屋選択)
    (予約) --> (部屋選択)
    (変更) --> (部屋選択)
    (部屋選択) --> (支払い)
    (予約) --> (オプション)
    (キャンセル) --> (オプション)
    (変更) --> (オプション)
    (オプション) ..> (追加)
    (オプション) ..> (含まれる)
    (オプション) ..> (拡張)
    (ログイン) <-- (customer)
    (ログアウト) <-- (customer)
    (ログイン) <-- (admin)
    (ログアウト) <-- (admin)
  }
  @enduml
---

# ホテル予約サイト

あなたは、ホテル予約のウェブアプリケーションを開発しています。  
ホテル予約システムでは、顧客はホテルを検索し、部屋を選択し、予約、キャンセル、変更ができます。また、管理者はホテル情報を編集、削除、追加、表示することができます。ログインとログアウトも含まれています
このアプリケーションのユースケース図を作成してください。
